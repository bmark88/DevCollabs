import { useEffect, useReducer } from "react";
import axios from "axios";


export default function useApplicationData() {

    const fetchGroups = () => {
        Promise.all([
        };
        try {
            await axios.get(`/groups${id}`, appointment);
}
}